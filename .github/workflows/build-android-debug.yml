name: Build NeuralBridge Chinese Debug APK

on:
  workflow_dispatch:
  push:
    paths:
      - 'android/**'
      - '.github/workflows/build-android-debug.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.13
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: Apply Simplified Chinese UI localization
        run: python3 android/tools/localize_zh_cn.py

      - name: Apply Android 14+ MediaProjection FGS FIX1
        run: python3 android/tools/fix_media_projection_fgs.py

      - name: Guard protocol and MediaProjection identifiers
        run: |
          grep -q '"ALL"' android/app/src/main/kotlin/com/neuralbridge/companion/MainActivity.kt
          grep -q 'MCP:' android/app/src/main/kotlin/com/neuralbridge/companion/MainActivity.kt
          grep -q 'FOREGROUND_SERVICE_TYPE_MEDIA_PROJECTION' android/app/src/main/kotlin/com/neuralbridge/companion/service/NeuralBridgeAccessibilityService.kt
          grep -q 'android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION' android/app/src/main/AndroidManifest.xml
          grep -q 'specialUse|mediaProjection' android/app/src/main/AndroidManifest.xml

      - name: Build debug APK
        working-directory: android
        run: gradle --no-daemon assembleDebug

      - name: Verify APK
        run: |
          find android -type f -name '*.apk' -print
          test -n "$(find android -type f -name '*.apk' -print -quit)"

      - name: Upload Chinese FIX1 APK
        uses: actions/upload-artifact@v4
        with:
          name: NeuralBridge-zh-CN-FIX1-debug-apk
          path: android/**/build/outputs/apk/debug/*.apk
          if-no-files-found: error
